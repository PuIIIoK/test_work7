# DiveSea NFT Marketplace — Техническая документация

## 📋 О проекте

**DiveSea** — это веб-приложение NFT маркетплейса, представляющее собой адаптивную landing page с интерактивным слайдером NFT карточек. Проект разработан на основе Figma-макета и демонстрирует современный подход к frontend-разработке.

### Назначение
- Демонстрация NFT коллекций
- Отображение актуальных ставок и таймеров окончания торгов
- Полностью адаптивный интерфейс для всех устройств

---

## 🛠️ Технологический стек

### Основные технологии

| Технология | Версия | Назначение |
|------------|--------|------------|
| **Next.js** | 14.x | React-фреймворк с SSR/SSG |
| **React** | 18.x | UI библиотека |
| **TypeScript** | 5.x | Типизация JavaScript |
| **Redux Toolkit** | 2.x | Управление состоянием |
| **SCSS Modules** | — | CSS-модули с препроцессором |
| **Framer Motion** | 11.x | Анимации |
| **Swiper.js** | 11.x | Слайдер/карусель |

### Инфраструктура

| Технология | Назначение |
|------------|------------|
| **Docker** | Контейнеризация |
| **ESLint** | Линтинг кода |
| **Node.js 18+** | Runtime |

---

## 🏗️ Архитектура

### Паттерны проектирования

1. **Feature-Sliced Design** — компоненты организованы по функциональному назначению
2. **Container/Presentational** — разделение логики и представления
3. **Redux Ducks** — организация Redux-логики в slices

### Структура компонентов

```
components/
├── Header/
│   ├── Header.tsx          # Логика компонента
│   └── Header.module.scss  # Изолированные стили
├── Footer/
│   ├── Footer.tsx
│   └── Footer.module.scss
├── NFTCard/
│   ├── NFTCard.tsx         # Карточка NFT
│   ├── Timer.tsx           # Компонент таймера
│   └── NFTCard.module.scss
└── Slider/
    ├── NFTSlider.tsx       # Слайдер карточек
    └── NFTSlider.module.scss
```

---

## 🔄 State Management

### Redux Store

Используется **Redux Toolkit** для управления глобальным состоянием:

```typescript
// store/slices/nftSlice.ts
interface NFTState {
  items: NFT[];
  loading: boolean;
  error: string | null;
}
```

### Типизированные хуки

```typescript
// hooks/redux.ts
export const useAppDispatch = () => useDispatch<AppDispatch>();
export const useAppSelector: TypedUseSelectorHook<RootState> = useSelector;
```

---

## 🎨 Стилизация

### SCSS Modules

Каждый компонент имеет изолированные стили:

```scss
// Component.module.scss
.container {
  max-width: 1200px;
  margin: 0 auto;
}
```

### Глобальные переменные

```scss
// globals.scss
:root {
  --foreground-rgb: 26, 26, 26;
  --background-start-rgb: 241, 241, 241;
}
```

### Адаптивность

Media queries для основных breakpoints:

| Breakpoint | Ширина | Устройство |
|------------|--------|------------|
| Desktop | 1200px+ | ПК |
| Tablet | 768px-1199px | Планшет |
| Mobile | < 768px | Телефон |

---

## 🌐 API интеграция

### CoinGecko NFTs API

Используется публичный API для получения списка NFT:

```typescript
// lib/api/nfts.ts
const response = await fetch(
  'https://api.coingecko.com/api/v3/nfts/list'
);
```

### Генерация данных

Дополнительные данные (таймеры, ставки) генерируются на клиенте:

```typescript
// lib/utils/generateNFTData.ts
export function generateNFTData(name: string): NFT {
  return {
    name,
    endTime: generateRandomEndTime(),
    currentBid: generateRandomBid(),
    // ...
  };
}
```

---

## ✨ Ключевые функции

### 1. NFT карточки
- Отображение изображения, названия, ставки
- Живой обратный отсчёт
- Hover-анимации

### 2. Слайдер
- Горизонтальная прокрутка карточек
- Навигационные стрелки
- Touch/swipe поддержка на мобильных

### 3. Адаптивный Header
- Desktop: логотип + навигация
- Mobile: логотип + бургер-меню
- Fullscreen мобильное меню с анимацией

### 4. Footer
- Разный текст copyright для desktop/mobile
- Навигационные ссылки

---

## 🐳 Docker деплой

### Dockerfile

```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
RUN npm run build
EXPOSE 3000
CMD ["npm", "start"]
```

### Команды

```bash
# Сборка
docker build -t divesea-nft .

# Запуск
docker run -p 3000:3000 divesea-nft
```

---

## 📱 Браузерная совместимость

| Браузер | Поддержка |
|---------|-----------|
| Chrome | ✅ Последние 2 версии |
| Firefox | ✅ Последние 2 версии |
| Safari | ✅ Последние 2 версии |
| Edge | ✅ Последние 2 версии |

---

## 📝 Разработка

### Рекомендации

1. Используйте TypeScript для всех новых файлов
2. Следуйте структуре SCSS Modules для стилей
3. Добавляйте типы в `types/` директорию
4. Используйте типизированные Redux хуки

### ESLint

```bash
npm run lint
```

---

## 📄 Лицензия

MIT License
